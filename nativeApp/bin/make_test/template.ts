export default () => `describe('TEST SUITE NAME', () => {
  describe(':: FUNCTION | COMPONENT NAME', () => {
    test('ASSERTION DESCRIPTION', () => {
      const actual = ;
      expect(actual).toEqual();
    });
  });
});

`;
